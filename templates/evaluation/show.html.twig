{% extends 'RRH.html.twig' %}

{% block title %}Evaluation{% endblock %}

{% block body %}
    <h1>Evaluation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Note</th>
                <td>{{ evaluation.note }}</td>
            </tr>
            <tr>
                <th>Avis</th>
                <td>{{ evaluation.avis }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default"  role="button" href="{{ path('app_evaluation_index') }}">back to list</a>

    <a class="btn btn-primary"  role="button" href="{{ path('app_evaluation_edit', {'idEvaluation': evaluation.idEvaluation}) }}">edit</a>

    {{ include('evaluation/_delete_form.html.twig') }}
{% endblock %}
